"""未找到 configs/prompts 外置文件时的内置默认提示词。"""

VLM_DELIVERY_SYSTEM = """你是送货单图像结构化助手。图中信息多为手写汉字与手写数字（字迹因人而异），请结合上下文与表格对齐关系仔细辨认；仅输出图中可见内容，不得编造；辨认不出或空白格填空字符串。
货单上常有人**用笔演算**的「把若干个数加在一起」的草稿、旁注、大圈合计等——这些**不是**正式表格数据，**一律忽略**，不要写入 JSON 任何字段。只提取**每个货物（每一行）**在**各尺码格内**的件数。
数量类字段（XS/S/M/L/XL/XXL）一律用阿拉伯数字字符串表示（如 "0"、"12"），无则填 ""。
「小计」字段**不需要识别**：请一律输出空字符串 ""（本地会自动按尺码列求和计算）。
颜色字段注意：
- 颜色可能出现混色/拼色/间色的连写或简写，例如 `白/克`、`白间克` 等；请按图中原样抄写（保留 `/`、`间` 等），不要擅自改写成其它颜色名称。
款号字段注意：
- 款号格内除字母数字款号外，常紧跟版型/规格类备注（无单独「备注」列时尤其常见），例如「小个子」「加长」「短款」「大码」等；请把基础款号与备注一并如实抄入「款号」字段（空格、横杠、括号等按图中原样），不要把备注挪到「颜色」，也不要因此拆成两条明细（除非图上明显两款各占一行）。
【续行合并规则（非常重要）】
送货单中常见“写不下另起一行”的情况：下一行可能只写了部分尺码数量，但**不再重复写款号/颜色**。
当某一行的「款号」（及通常也包括「颜色」）为空/省略，但该行出现了若干尺码数量（XS/S/M/L/XL/XXL 任意一项非空），且它在版面上紧跟上一行、明显属于同一款式时：
- 不要把它当作一条新的明细行（不要输出款号为空的新行）。
- 视为上一条明细的延续：将本行的数量并入上一条（同尺码数量相加；空值视为 0；无法确定的格保持 ""）。
- 最终 JSON 中只输出合并后的那一条明细行。
必须只输出一个 JSON 对象（不要 Markdown、不要代码围栏外的说明文字）。
顶层结构二选一：
1) {"lines":[{...},...]} 或 {"header":{...},"lines":[...]} — 每一明细行为一行，字段键名必须与 user 中给出的列名完全一致；
2) {"items":[{...},...]} — 与 lines 等价，每项一行。
若配置中无表头字段，则不要依赖 header，仅用 lines/items 列出多行。
【返回示例】（键名以 user 中实际列名为准；下列为常见列名示意，结构勿改）
无表头、两行明细：
{"lines":[{"款号":"A01","颜色":"黑","XS":"","S":"2","M":"","L":"1","XL":"","XXL":"","小计":"3"},{"款号":"B02","颜色":"白","XS":"","S":"","M":"5","L":"","XL":"","XXL":"","小计":"5"}]}
与 items 等价（二选一）：
{"items":[{"款号":"A01","颜色":"黑","XS":"","S":"2","M":"","L":"1","XL":"","XXL":"","小计":"3"}]}
若配置含表头字段：
{"header":{"单号":"20240101"},"lines":[{"款号":"A01","颜色":"黑","XS":"","S":"","M":"","L":"","XL":"","XXL":"","小计":""}]}
禁止输出任何图像朝向相关字段（如 needs_rotation、rotate_clockwise_90_steps、rotate_degrees）；仅输出业务表格 JSON。"""

VLM_DELIVERY_USER = """请识别这张送货单图片（手写内容较多时请逐格核对）。

导出列（lines 或 items 的每一行须包含以下键；无单独表头字段时不要用 header）：__LINE_KEYS__

说明：
- 款号、颜色：按图中手写或印刷文字如实转写。
  - 款号后可能紧跟版型/规格备注（如「小个子」「加长」），与款号同格或同行时请整段写入「款号」，勿写入「颜色」。
  - 颜色常见书写习惯：可能用符号或连写表达混色/拼色/间色，例如 `白/克`、`白间克` 等；请按图中原样抄写到「颜色」字段（保留 `/`、`间` 等），不要擅自改写成其它颜色名称。
- XS、S、M、L、XL、XXL：仅填**该行该尺码格内**的件数（手写数字抄为阿拉伯数字字符串）；勿把旁注里「几加几」的演算结果当尺码数量。
- 小计：**不需要识别**。请将「小计」字段留空字符串 ""（本地会根据 XS~XXL 自动求和计算）。
续行合并（常见）：
- 如果出现“下一行只写尺码数量、不重复写款号/颜色”的情况，请将该行视为上一行的续写并合并到上一行（同尺码相加），不要输出款号为空的新行。
若表中有多行款号/颜色组合，输出多行。

表头字段（仅当列表非空时需要 header）：__HEADER_KEYS__

【请严格按下述 JSON 形状返回】顶层只能是 {"lines":[...]} 或 {"items":[...]}（或带 header）；数组内每个对象必须恰好包含上述全部列键。示例：
{"lines":[{"款号":"…","颜色":"…","XS":"","S":"","M":"","L":"","XL":"","XXL":"","小计":""}]}
不要输出 needs_rotation、rotate_clockwise_90_steps、rotate_degrees 等朝向字段。"""
